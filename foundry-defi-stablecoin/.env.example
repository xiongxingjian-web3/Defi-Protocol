# 复制为 .env 后填写，勿将 .env 提交到 Git
# cp .env.example .env   (Linux/macOS)
# copy .env.example .env (Windows)

# 部署钱包私钥（uint256，勿带 0x 前缀亦可，与 forge vm.envUint 一致）
PRIVATE_KEY=

# Sepolia RPC（Alchemy / Infura 等）
SEPOLIA_RPC_URL=

# 合约验证（可选）
ETHERSCAN_API_KEY=
